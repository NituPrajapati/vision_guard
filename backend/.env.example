# JWT Secret Key - Use a strong random string in production
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-secret-key-change-this-in-production

# MongoDB Configuration
# For local development, use the default below
# For production (e.g., MongoDB Atlas), use your connection string
MONGO_URI=mongodb://localhost:27017/
DB_NAME=visionguard

# Environment
FLASK_ENV=development

# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Google OAuth URIs (optional - defaults are standard Google endpoints)
GOOGLE_AUTH_URI=https://accounts.google.com/o/oauth2/auth
GOOGLE_TOKEN_URI=https://oauth2.googleapis.com/token
GOOGLE_USERINFO_URI=https://www.googleapis.com/oauth2/v2/userinfo

# Frontend URLs
# For local development:
FRONTEND_BASE_3000=http://localhost:3000
FRONTEND_BASE_5173=http://localhost:5173
FRONTEND_BASE=http://localhost:5173
GOOGLE_REDIRECT_URI=http://localhost:5173/auth/callback

# For production (e.g., Render), set these to your deployed frontend URL:
# FRONTEND_BASE=https://your-frontend-app.onrender.com
# GOOGLE_REDIRECT_URI=https://your-frontend-app.onrender.com/auth/callback
